# seanghods.xyz

Personal portfolio — a single-page site built with React, TypeScript, Vite, and Tailwind CSS. Deployed on Vercel.

```bash
yarn         # install
yarn dev     # local dev server
yarn build   # type-check + production build
yarn lint    # eslint
```
